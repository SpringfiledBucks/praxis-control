# Linux 原生 GUI 验收状态

状态：VERIFIED（Linux 无桌面自动验收范围）

时间：2026-07-29

## 已完成

- GTK4 + libadwaita + GJS 原生壳覆盖服务发现、API v1 校验、工作台、图谱计数、打开 Web、刷新和安全关闭；
- XDG runtime/state 路径与后端实现保持一致；
- 提供 GNOME desktop 文件、Meson 安装入口和无桌面 `--smoke-test`；
- 纯逻辑测试覆盖 XDG 路径、API 不兼容拒绝与工作台映射；
- Gitea Actions 增加 Ubuntu GTK 依赖安装和 Xvfb/DBus 启动检查；
- 2026-07-29 在 NAS 一次性、无卷 Alpine 3.23 容器中解析并运行 GJS 1.86、GTK 4.20、libadwaita 1.8 与 Soup 3.6，Xvfb/DBus 下真实启动退出码为 0；
- 新增连接型 GTK 冒烟验收：隔离模拟 API 提供运行时状态，客户端必须在 5 秒内完成 `/api/meta`、`/api/dashboard`、`/api/graph` 三个请求并输出 `PRAXIS_GUI_SMOKE_CONNECTED`。NAS 实测 `Linux GTK connected smoke: PASS`；
- CI 已改为执行连接型验收；仅创建窗口、停留在离线状态不再算通过；
- 一次性容器和 NAS 临时目录已删除，未创建卷、未修改镜像或现有服务。
- 客户端已连接真实临时 PGlite 服务，完成 API v1、工作台和图谱读取；随后从原生 GUI 安全关闭并确认服务进程正常退出、运行时状态文件移除；
- AT-SPI 树已确认窗口、在线状态、指标、列表及“打开 Web”“刷新”“安全关闭服务”按钮；验收脚本通过可访问性动作实际触发刷新和安全关闭；
- Noto CJK 字体下生成并人工检查实际 GTK 截图，中文可读、布局无重叠，证据见 [`2026-07-29-linux-gui-acceptance.png`](2026-07-29-linux-gui-acceptance.png)；
- Meson 已在一次性前缀完成安装，launcher、desktop 文件、可缩放图标、图标缓存和桌面数据库均通过；安装后的 launcher 在 Xvfb/DBus 中真实启动成功；
- 修复了归档到 Linux 时 desktop 文件被转换为 CRLF 的问题，`clients/linux/**` 现由 `.gitattributes` 强制 LF，desktop 分类收敛为单一 `Utility`。

## 当前限制

- 本机没有 WSL/Linux 发行版，也没有 Docker；
- NAS 主机没有常驻安装 GJS、GTK4、libadwaita、Meson、Flatpak 或图形会话；运行依赖仅存在于已删除的一次性容器；
- Gitea 目标仓库和 runner 尚未建立，工作流尚未由 runner 执行；同等命令已在 NAS 一次性 Alpine 3.23 环境手动重复通过；
- 尚未在带 GNOME/KDE 会话的长期 Linux 桌面做人工鼠标/键盘验收，也未制作 Flatpak；这些属于发行与桌面集成扩展，不阻断当前 Linux 原生客户端代码、安装包、可访问性和真实服务交互的 VERIFIED 结论。

当前 Linux 原生客户端在 MVP 范围内已具备可复现的运行、安装、渲染、可访问性、刷新和安全关闭证据。
