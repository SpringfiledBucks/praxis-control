# 本机运行时生命周期与跨客户端发现审计

状态：VERIFIED

日期：2026-07-30

## 范围与保护对象

本轮由外部 Windows 用户反馈的固定端口冲突触发，审查轻量版的端口分配、服务发现、启动失败诊断、单实例、状态文件、CLI/GUI API 认证和安全关闭。保护对象是 PGlite 事实数据、审计历史、运行时控制令牌以及其他本机应用的固定端口。

全量版的容器内端口、主机回环发布和反向代理入口继续保持显式固定配置；本轮不改变 PostgreSQL 拓扑、生产认证和跨设备同步边界。

## 已确认并修复

1. **固定端口冲突**：轻量本机默认不再绑定 `4310`，而是直接监听端口 `0`，由操作系统原子分配动态回环端口；不扫描低端候选端口。
2. **检查与使用竞态**：服务不执行“探测空闲端口后关闭再重开”，监听成功的 socket 就是正式 socket。
3. **跨客户端发现**：实际 URL 写入 `service.json`，Web 启动器、CLI、TUI、Windows GUI 和 Linux GUI 均读取同一状态。
4. **启动错误丢失**：后台标准输出和错误追加到用户数据目录的 `logs/service.log`；CLI 监视子进程，提前退出时报告退出码和日志路径，等待上限从 20 秒调整为 60 秒。
5. **数据库并发打开窗口**：服务在创建数据库前取得按运行目录划分的独占启动锁；迁移和种子命令在 PGlite 模式下也使用相同锁。正常关闭和启动失败均清理锁与状态。
6. **陈旧状态误判**：运行状态除 PID 存活外，还必须使用状态文件令牌成功读取受保护的 `/api/system/runtime` 实例确认端点，否则清理陈旧状态。
7. **访问模式契约漂移**：CLI 与 Windows/Linux GUI 统一携带运行时 API 令牌；服务允许该本机令牌访问版本化 API，而外部 Web 仍执行密码会话或可信身份头认证。
8. **显式端口边界**：设置 `APP_PORT` 时严格使用该固定端口；PostgreSQL 或非本机访问模式未设置固定端口时配置校验直接失败。

## 当前验证证据

- `npm run typecheck`：通过；
- `npm test`：10 个测试文件通过、1 个跳过；34 项通过、2 项跳过；
- `npm run build`：通过；
- `npm run test:linux-client`：3 项通过，Linux GUI 与真实服务脚本语法检查通过；
- `npm run test:windows-gui-e2e`：通过，最终复核动态端口 `51752`，原生保存与审计事件闭环成功；
- `npm run test:windows-package`：通过，最终复核动态端口 `63785`，便携包 2458 个文件，审计事件闭环成功；
- 本机隔离生命周期检查：操作系统分配端口 `55907`/`49235`；第二服务被启动锁拒绝；安全关闭后 `service.json` 与 `service.lock` 均清理；
- 固定端口冲突检查：持有测试端口 `64040` 后，CLI 在约 3.08 秒内报告子进程退出和日志路径，日志保留完整 `EADDRINUSE` 堆栈。

端口数值只是本次隔离运行证据，不是产品固定值。

## 剩余风险和门槛

- GTK/AT-SPI 真实 GUI 由 Gitea Linux 作业在 Xvfb/DBus 环境执行，本机 Windows 不重复模拟该环境。
- 启动锁是用户目录中的原子文件加 PID 校验，不是操作系统持有式文件锁。崩溃后的死 PID 可自动恢复；极端 PID 复用会安全地拒绝启动，需要 `doctor` 辅助人工确认，不会冒险并发打开数据库。
- `service.log` 当前为追加文件，尚未实现大小轮换；日志内容不写控制令牌，但长期运行仍需后续增加有界轮换。
- 远程设备上的原生 GUI 尚未提供独立的远程服务配置流程；当前全量版跨设备共用入口仍是经过认证的 Web。这不影响本机轻量版验收，但属于后续客户端范围。

## Gitea CI 回连修复

提交首次推送后，Gitea Actions run 81 的三个作业均在 checkout 阶段失败。日志确认 Gitea 重启后的 `github.server_url` 为 `http://localhost:3000`，作业容器因此错误连接自身，而不是 Gitea 服务；此前 run 80 已有同样失败，说明这是既有 CI 环境漂移，不是本轮代码测试失败。

工作流改为给每个作业容器声明 Docker `host-gateway`，并通过通用的 `http://host.docker.internal:3000` 回连宿主机发布的 Gitea 端口。该地址不包含 NAS、账号或受控网络信息。已使用同一 CI 镜像在 NAS 创建一次性 `--rm` 容器，成功读取 Gitea 1.26.1 版本端点；最终状态仍以修复提交触发的完整 Gitea 作业为准。

修复提交触发的 run 82 已证明三个作业均能完成 checkout；其中 PostgreSQL 作业随后暴露测试夹具未给全量模式提供显式 `APP_PORT` 的契约漂移，并在初始化失败时产生二次清理错误。测试夹具已改为显式固定端口，且清理逻辑允许初始化未完成。最终 run 83 的 `verify`、`linux-gui-smoke`、`postgres-contract` 三个作业全部通过，因此本报告状态提升为 VERIFIED。
