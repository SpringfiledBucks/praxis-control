# ADR-0004：追加式审计与纠正事件

状态：ACCEPTED

日期：2026-07-28

创建、分析、人工选择、覆盖、状态转换、结果、复盘、导出和规则变更均追加审计事件。历史错误通过纠正事件修正，不无痕删除。

事件保存对象、类型、负载、规则版本、前序哈希和事件哈希。哈希链只用于发现篡改，不能替代权限、事务、备份和恢复演练。

每个对象的 `audit_heads` 行在事务内加锁并推进链头，防止并发写入产生分叉。校验器不依赖时间戳排序，而是按前序哈希重建链并检查负载篡改、缺失前驱、多根、分叉和断链。
